# 上游根 .gitignore 有全局 `*.md` 规则（仅 !README.md 例外），会把 LOCAL_CHANGES.md
# 这类本地文档静默忽略掉。这里用子目录规则把它们捞回来。
#
# 这样做的意义：不修改上游的任何文件，冲突面为零。
# （git 规则：子目录 .gitignore 优先级高于父目录，且被排除的是「文件」而非「目录」，
#   所以这里的 !*.md 能生效。）
!*.md

# 推送凭据绝不能进版本库（私钥一旦 commit 就等于泄露，且历史里删不掉）
.ssh/
*.token

# 原版镜像专有脚本（删库后唯一副本，MIT 许可再分发需保留版权声明）。
# 默认不提交；确需备份到私有仓库时用 git add -f。
upstream-scripts/*.py
